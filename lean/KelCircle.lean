-- KelCircle: Lean 4 formalization of the kel-circle protocol invariants
--
-- The kel-circle protocol extends KERI with a global sequence that provides
-- canonical ordering for interaction events across multiple independent KELs.

import KelCircle.GlobalSequence
import KelCircle.Events
import KelCircle.Fold
import KelCircle.Invariants
